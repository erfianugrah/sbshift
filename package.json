{
  "name": "pgshift",
  "version": "0.1.0",
  "description": "Typed CLI orchestrator for cross-region Supabase project migration via logical replication.",
  "license": "MIT",
  "author": "Erfi Anugrah <erfi.anugrah@gmail.com>",
  "type": "module",
  "bin": {
    "pgshift": "./src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test",
    "test:integration": "bun start rehearse integration",
    "typecheck": "tsc --noEmit",
    "check": "biome check --write .",
    "lint": "biome check .",
    "test:live": "bun run test/live.harness.ts",
    "test:scale": "docker compose -f docker-compose.test.yml run --rm -e ROWS=1000000 runner sh -c 'bun install --frozen-lockfile && bun run test/scale.harness.ts'"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "postgres": "^3.4.7",
    "yaml": "^2.8.1",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.4",
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.3"
  }
}
