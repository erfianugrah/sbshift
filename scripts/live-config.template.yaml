# Generated by scripts/live-supabase-test.sh — do not edit by hand.
source:
  ref: __SRC_REF__ # pgshift-src (eu-central-1)
target:
  ref: __TGT_REF__ # pgshift-tgt (eu-west-1)

replication:
  publication: pgshift_pub
  slot: pgshift_slot
  subscription: pgshift_sub
  copyData: true
  tables:
    - public.users
    - public.documents
    - public.events
    - public.audit

reconcile:
  tables:
    - name: public.users
    - name: public.documents
    - name: public.events
    - name: public.audit

watchdog:
  maxRetainedWalMb: 4096
  pollIntervalSec: 5
  syncTimeoutMin: 30

configSync:
  auth: true
  realtime: true
  postgrest: true
  storage: true
  dbPooler: true
  dbPostgres: false

storage:
  buckets: []

functions:
  enabled: false
